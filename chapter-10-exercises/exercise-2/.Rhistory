employees <- paste("Employee", 1:100)
print(employees)
salaries_2017 <- runif(100,40000, 50000)
# Create a vector of 100 annual salary adjustments between -5000 and 10000.
# (A negative number represents a salary decrease due to corporate greed)
# Again use the `runif()` function to pick 100 random numbers in that range.
adjusments <- runif(100, -5000, 10000)
# Create a data frame `salaries` by combining the 3 vectors you just made
# Remember to set `stringsAsFactors=FALSE`!
salaries <- data.frame(employees, salaries_2017, adjusments)
salaries$salaries_2018 <- salaries$salaries_2017 + salaries$adjusments
View(salaries)
# Add a column to the `salaries` data frame that has a value of `TRUE` if the
# person got a raise (their salary went up)
salaries$got_raise <- salaries$adjusments > 0
# What was the 2018 salary of Employee 57
salaries[57 ,"salaries_2018"]
# What was the 2018 salary of Employee 57
print(salaries[salaries$employees == 57 ,"salaries_2018"])
# What was the 2018 salary of Employee 57
print(salaries[salaries$employees == "Employees 57" ,"salaries_2018"])
# How many employees got a raise?
print(nrow(salaries[salaries$got_raise == TRUE,]))
# What was the 2018 salary of Employee 57
print(salaries[salaries$employees == "Employees 58" ,"salaries_2018"])
# What was the 2018 salary of Employee 57
print(salaries[salaries$employees == "Employees 57" ,salaries_2018])
# What was the 2018 salary of Employee 57
print(salaries[salaries$employees == "Employees 57" ,"salaries_2018"])
salaries[ salaries$adjusments == max(salaries$adjusments), "adjustments"]
# What was the dollar value of the highest raise?
biggest_raise <- max(salaries$adjusments)
salaries[ salaries$adjusments == max(salaries$adjusments), "adjustments"]
# What was the dollar value of the highest raise?
biggest_raise <- max(salaries$adjusments)
employees <- paste("Employee", 1:100)
print(employees)
salaries_2017 <- runif(100,40000, 50000)
# Create a vector of 100 annual salary adjustments between -5000 and 10000.
# (A negative number represents a salary decrease due to corporate greed)
# Again use the `runif()` function to pick 100 random numbers in that range.
adjusments <- runif(100, -5000, 10000)
# Create a data frame `salaries` by combining the 3 vectors you just made
# Remember to set `stringsAsFactors=FALSE`!
salaries <- data.frame(employees, salaries_2017, adjusments)
salaries$salaries_2018 <- salaries$salaries_2017 + salaries$adjusments
# Add a column to the `salaries` data frame that has a value of `TRUE` if the
# person got a raise (their salary went up)
salaries$got_raise <- salaries$adjusments > 0
# What was the 2018 salary of Employee 57
print(salaries[salaries$employees == "Employees 57" ,"salaries_2018"])
# How many employees got a raise?
print(nrow(salaries[salaries$got_raise == TRUE,]))
# What was the dollar value of the highest raise?
#biggest_raise <- max(salaries$adjusments)
salaries[ salaries$adjusments == max(salaries$adjusments), "adjustments"]
# What was the dollar value of the highest raise?
#biggest_raise <- max(salaries$adjusments)
salaries[ salaries$adjusments == max(salaries$adjusments), "adjustments"]
# What was the dollar value of the highest raise?
#biggest_raise <- max(salaries$adjusments)
salaries[ salaries$adjusments == max(salaries$adjusments), "adjustments", "employees"]
# Create a data frame `salaries` by combining the 3 vectors you just made
# Remember to set `stringsAsFactors=FALSE`!
salaries <- data.frame(employees, salaries_2017, adjusments, stringsAsFactors = FALSE)
salaries$salaries_2018 <- salaries$salaries_2017 + salaries$adjusments
# Add a column to the `salaries` data frame that has a value of `TRUE` if the
# person got a raise (their salary went up)
salaries$got_raise <- salaries$adjusments > 0
# What was the 2018 salary of Employee 57
print(salaries[salaries$employees == "Employees 57" ,"salaries_2018"])
# How many employees got a raise?
print(nrow(salaries[salaries$got_raise == TRUE,]))
# What was the dollar value of the highest raise?
#biggest_raise <- max(salaries$adjusments)
salaries[ salaries$adjusments == max(salaries$adjusments), "adjustments", "employees"]
# What was the dollar value of the highest raise?
#biggest_raise <- max(salaries$adjusments)
salaries[ salaries$adjusments == max(salaries$adjusments), "adjustments", "employees"]
# Add a column to the `salaries` data frame that has a value of `TRUE` if the
# person got a raise (their salary went up)
salaries$got_raise <- salaries$adjusments > 0
# What was the 2018 salary of Employee 57
salaries[salaries$employees == "Employees 57" ,"salaries_2018"]
# What was the 2018 salary of Employee 57
salaries[salaries$employees == "Employees 57" , "salaries_2018"]
print(employees)
salaries_2017 <- runif(100,40000, 50000)
# Create a vector of 100 annual salary adjustments between -5000 and 10000.
# (A negative number represents a salary decrease due to corporate greed)
# Again use the `runif()` function to pick 100 random numbers in that range.
adjusments <- runif(100, -5000, 10000)
# Create a data frame `salaries` by combining the 3 vectors you just made
# Remember to set `stringsAsFactors=FALSE`!
salaries <- data.frame(employees, salaries_2017, adjusments, stringsAsFactors = FALSE)
salaries$salaries_2018 <- salaries$salaries_2017 + salaries$adjusments
# Add a column to the `salaries` data frame that has a value of `TRUE` if the
# person got a raise (their salary went up)
salaries$got_raise <- salaries$adjusments > 0
# What was the 2018 salary of Employee 57
salaries[salaries$employees == "Employees 57" , "salaries_2018"]
# What was the 2018 salary of Employee 57
salaries[salaries$employees == "Employees 57" , "salaries_2018"]
print(salaries_2018)
print(salaries$salaries_2018 <- salaries$salaries_2017 + salaries$adjusments)
# What was the 2018 salary of Employee 57
salaries[ salaries$employees == "Employees 57" , "salaries_2018"]
# How many employees got a raise?
nrow(salaries[salaries$got_raise == TRUE,])
# What was the 2018 salary of Employee 57
salaries[ salaries$employees == "Employees 57" , "salaries_2018"]
salaries[ salaries$employees == "Employees 57" , "salaries_2018"]
salaries$salaries_2018 <- salaries$salaries_2017 + salaries$adjusments)
salaries$salaries_2018 <- salaries$salaries_2017 + salaries$adjusments
salaries$salaries_2018 <- salaries$salaries_2017 + salaries$adjusments
# What was the dollar value of the highest raise?
#biggest_raise <- max(salaries$adjusments)
salaries[ salaries$adjusments == max(salaries$adjusments), "adjustments", "employees"]
# What was the 2018 salary of Employee 57
salaries[ salaries$employees == "Employees 57" , "salaries_2018"]
salaries$salaries_2018 <- salaries$salaries_2017 + salaries$adjusments
# What was the 2018 salary of Employee 57
salaries[ salaries$employees == "Employees 57" , "salaries_2018"]
# What was the 2018 salary of Employee 57
salaries[ salaries$employees == "Employees 57" , "salaries_2018"]
# How many employees got a raise?
nrow(salaries[salaries$got_raise == TRUE,])
# What was the dollar value of the highest raise?
#biggest_raise <- max(salaries$adjusments)
salaries[ salaries$adjusments == max(salaries$adjusments), "adjustments", "employees"]
# What was the 2018 salary of Employee 57
salaries[ salaries$employees == "Employee 57" , "salaries_2018"]
# What was the 2018 salary of Employee 57
salaries[ salaries$employees == "Employee 57" , "salaries_2018"]
# How many employees got a raise?
nrow(salaries[salaries$got_raise == TRUE,])
# What was the dollar value of the highest raise?
#biggest_raise <- max(salaries$adjusments)
salaries[ salaries$adjusments == max(salaries$adjusments), "adjustments", "employees"]
# What was the dollar value of the highest raise?
#biggest_raise <- max(salaries$adjusments)
salaries[ salaries$adjusments == max(salaries$adjusments), "adjustments", "employee"]
# What was the dollar value of the highest raise?
#biggest_raise <- max(salaries$adjusments)
salaries[ salaries$adjusments == max(salaries$adjusments), "adjustments", "Employee"]
# What was the 2018 salary of Employee 57
salaries[ salaries$employees == "Employee 57" , "salaries_2018"]
# How many employees got a raise?
nrow(salaries[salaries$got_raise == TRUE,])
# What was the dollar value of the highest raise?
#biggest_raise <- max(salaries$adjusments)
salaries[ salaries$adjusments == max(salaries$adjusments), "adjustments", "employees"]
paycut <- salares[salaries$got_raise == FALSE. ]
paycut <- salaries[salaries$got_raise == FALSE. ]
paycut <- salaries[salaries$got_raise == FALSE,  ]
mean(paycut$adjusments)
mean(paycut)
paycut <- salaries[salaries$got_raise == FALSE,  ]
mean(paycut)
mean(paycut$adjusments)
paycut <- salaries[salaries$got_raise == FALSE,  ]
mean(salaries[salaries$got_raise -- FALSE, "adjustments"])
mean(salaries[salaries$got_raise -- FALSE, "adjustments"])
mean(paycut$adjusments)
# What was the "name" of the employee who received the highest raise
salaries[ salaries$adjusments == max(employees), "adjustments", "employees"]
# What was the dollar value of the highest raise?
#biggest_raise <- max(salaries$adjusments)
salaries[ salaries$adjusments == max(salaries$adjusments), "adjustments", "employees"]
